/*
	프로시져라는 건 어떻게 설계하고,어떻게 사용하는건가 를 살펴보자.

	프로시져는 3가지를 결정하고 만들어야 한다.
		- 묶인 작업을 사용할때(호출할때) 불릴 이름 : 함수명,프로시져명,메서드명
		- 이 작업이 어떤 데이터타입을 만들어내기 위함인지 : 반환형, 리턴타입, 프로시져 결과물
		- 이 작업이 사용될때 전달받을 데이터들의 형태 : 전달인자 형태,매개변수 형태

	이렇게 설정을 하고 만들어둔 프로시져는 만들어진 형태로만 작동하게 된다.
*/
class Source02_Procedure {
	public static void main(String[] args) {
		// 1. 제곱값 계산해주는 프로시져를 pow 라는 이름을 붙여서 만들어두었기 때문에
		// 그 이름을 통해서 사용이 됨.
		// Math.POW(3, 5);
		// 2. 이 프로시져는 double 형 실수데이터를 만들기 위해 설계되있다.
		// String s = Math.pow(3, 5);
		int n = (int)Math.pow(3, 5);
		// 3. 이 프로시져는 작동을 시킬때 수치두개를 받아서 작동되게 설계되있다.
		Math.pow(3, 35);
	}
}
